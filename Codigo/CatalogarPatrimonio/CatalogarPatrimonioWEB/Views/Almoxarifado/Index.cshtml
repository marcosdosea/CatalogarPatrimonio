@model IEnumerable<CatalogarPatrimonioWEB.Models.AlmoxarifadoModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new {  id = item.IdAlmoxarifado }) |
                @Html.ActionLink("Details", "Details", new { id = item.IdAlmoxarifado }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.IdAlmoxarifado })
            </td>
        </tr>
}
    </tbody>
</table>
